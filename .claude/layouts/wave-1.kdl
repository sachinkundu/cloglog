layout {
    tab name="Phase 1 - Wave 1" {
        pane split_direction="vertical" {
            pane name="wt-board" {
                command "claude"
                args "--dangerously-skip-permissions" "Execute the plan at docs/superpowers/plans/2026-04-02-phase-1-board.md — you are in worktree wt-board. Read the plan, then execute all tasks in order. TDD: write failing test, run it, implement, run again, commit. When all tasks are done, run make quality, push your branch, and create a PR."
                cwd "/home/sachin/code/cloglog/.claude/worktrees/wt-board"
            }
            pane name="wt-frontend" {
                command "claude"
                args "--dangerously-skip-permissions" "Execute the plan at docs/superpowers/plans/2026-04-02-phase-1-frontend.md — you are in worktree wt-frontend. Read the plan, then execute all tasks in order. When all tasks are done, push your branch and create a PR."
                cwd "/home/sachin/code/cloglog/.claude/worktrees/wt-frontend"
            }
            pane name="wt-mcp" {
                command "claude"
                args "--dangerously-skip-permissions" "Execute the plan at docs/superpowers/plans/2026-04-02-phase-1-mcp.md — you are in worktree wt-mcp. Read the plan, then execute all tasks in order. TDD: write failing test, run it, implement, run again, commit. When all tasks are done, push your branch and create a PR."
                cwd "/home/sachin/code/cloglog/.claude/worktrees/wt-mcp"
            }
        }
    }
}
