layout {
    tab name="Phase 1 - Wave 2" {
        pane split_direction="vertical" {
            pane name="wt-gateway" {
                command "claude"
                args "--dangerously-skip-permissions" "Execute the plan at docs/superpowers/plans/2026-04-02-phase-1-gateway.md — you are in worktree wt-gateway. Read the plan, then execute all tasks in order. TDD: write failing test, run it, implement, run again, commit. When all tasks are done, run make quality. Then push and create a PR using the GitHub App bot identity as described in CLAUDE.md — NEVER push as the user's personal identity."
                cwd "/home/sachin/code/cloglog/.claude/worktrees/wt-gateway"
            }
            pane name="wt-agent" {
                command "claude"
                args "--dangerously-skip-permissions" "Execute the plan at docs/superpowers/plans/2026-04-02-phase-1-agent.md — you are in worktree wt-agent. Read the plan, then execute all tasks in order. TDD: write failing test, run it, implement, run again, commit. When all tasks are done, run make quality. Then push and create a PR using the GitHub App bot identity as described in CLAUDE.md — NEVER push as the user's personal identity."
                cwd "/home/sachin/code/cloglog/.claude/worktrees/wt-agent"
            }
            pane name="wt-document" {
                command "claude"
                args "--dangerously-skip-permissions" "Execute the plan at docs/superpowers/plans/2026-04-02-phase-1-document.md — you are in worktree wt-document. Read the plan, then execute all tasks in order. TDD: write failing test, run it, implement, run again, commit. When all tasks are done, run make quality. Then push and create a PR using the GitHub App bot identity as described in CLAUDE.md — NEVER push as the user's personal identity."
                cwd "/home/sachin/code/cloglog/.claude/worktrees/wt-document"
            }
        }
    }
}
