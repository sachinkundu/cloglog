# Demo: CI E2E Pipeline Design Spec (F-29)

This is a documentation-only PR containing a design spec. No executable code was added.

The spec is at `docs/superpowers/specs/2026-04-09-ci-e2e-pipeline.md`.

Key decisions: full-stack CI with path filters, required status check, Playwright deferred, fix pre-existing failures first, estimated 2-minute CI time.
