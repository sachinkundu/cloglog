# Work Log — close-wave t320-t332

Date: 2026-04-28
Worktrees closed: wt-t320-init-bootstrap, wt-t332-model-selection

---

## Shutdown Summary

| Worktree | Task | PR | Shutdown path | Notes |
|---|---|---|---|---|
| wt-t320-init-bootstrap | T-320 | #245 | cooperative | Worktree dir removed before close-wave; work logs archived by agent |
| wt-t332-model-selection | T-332 | #246 | cooperative | Full shutdown sequence |

---

## T-320: Phase 2.7 — replace init Step 2 mcp__cloglog__get_board with admin/backend bootstrap

*from work-log-T-320.md*

### What Was Done

Replaced `mcp__cloglog__get_board` in the `init` skill's Step 2 with a direct HTTP bootstrap that works on fresh projects where the MCP server isn't yet configured. Implemented a two-phase init flow:

- **Phase 1 (pre-MCP):** calls `POST /api/v1/projects` with `X-Dashboard-Key: $DASHBOARD_SECRET`, writes `CLOGLOG_API_KEY` to `~/.cloglog/credentials` (or prints it for export on multi-project machines), writes `project_id` + `backend_url` to `.cloglog/config.yaml`, requests restart.
- **Phase 2 (post-restart):** MCP server finds credentials and existing `project_id`; bootstrap is skipped; remaining setup steps proceed via MCP tools.

Also fixed the default backend URL from `:8000` to `127.0.0.1:8001` throughout.

### Files Changed

- `plugins/cloglog/skills/init/SKILL.md` — Step 2 completely replaced; Step 1c URL fixed; Step 3/4a templates updated for detected BACKEND_URL
- `docs/setup-credentials.md` — Two-phase init docs; single-slot warning updated; manual bootstrap section with backend_url write and multi-project conditional
- `tests/plugins/test_init_bootstrap_skill.py` — 10 new pin tests (all passing)

### Codex Review Rounds (6 rounds, 5 sessions)

- **Session 1**: Wrong endpoint, env var, skip condition
- **Session 2**: `localhost`→`127.0.0.1`, dual skip check (project_id + credentials)
- **Session 3**: backend_url not persisted, weaker YAML parser, single-slot guard
- **Session 4**: `CLOGLOG_DASHBOARD_KEY` still in single-slot message, manual bootstrap missing backend_url write
- **Session 5**: Hard-stop guard before project creation blocks multi-project path

### Residual TODOs

- Multi-project credential format is still a gap — the `MULTI_PROJECT` branch is a workaround; proper per-project credentials file tracked in portability audit
- `docs/setup-credentials.md` manual bootstrap conditional could be extracted to a helper script

---

## T-332: Model selection per task — encode at spec time, pass --model on launch

*from work-log-T-332.md*

### What Was Done

Added per-task model selection end-to-end:

1. **DB migration** — `tasks.model` column (String(100), nullable) via Alembic revision `a4b5c6d7e8f9`
2. **Board schemas** — `model: str | None = None` on `TaskCreate`, `TaskUpdate`, `TaskResponse`, `SearchResult`, `ActiveTaskItem`
3. **Agent schemas** — `model: str | None = None` on `TaskInfo`, `StartTaskResponse`; `start_task` returns `task.model`
4. **Repository** — `create_task` accepts `model=`; `update_task` removes `if value is not None` guard so explicit `null` clears the column
5. **OpenAPI contract** — added `model` to all touched schemas; regenerated `frontend/src/api/generated-types.ts`
6. **MCP server** — `model?: string` in `create_task`, `model?: string | null` in `update_task`
7. **Launch skill** — Step 4e writes `.cloglog/task-model`; launch.sh passes `--model` via `_MODEL_FLAG`; Supervisor Relaunch Flow updates task-model before each continuation
8. **Pin tests** — 4 Python + 4 TypeScript tests

### Codex Review Rounds (4 sessions)

- **Session 1** — `ActiveTaskItem` missing `model`; OpenAPI out of sync
- **Session 2** — Feature launch path had no `TASK_MODEL` source
- **Session 3** — MCP `create_task`/`update_task` missing `model` parameter
- **Session 4** — `update_task` repository dropped explicit `null`

### Residual TODOs

- Frontend task-detail model display + inline edit (out of scope)
- `/cloglog launch T-NNN --model <override>` syntax (not implemented)
- Per-task_type default model fallback spec→opus, impl→sonnet when `task.model` is null (not implemented)
- The `update_task` null-clear change is broad — affects all nullable task fields; correct but reviewers should know

---

## Integration

- Quality gate passed (1013 tests, 88.45% coverage)
- MCP dist rebuilt — tool surface unchanged
- No integration issues post-merge

## State After This Wave

- F-53 (Plugin Portability): T-320 (Phase 2.7) and T-332 (model selection) merged
- Launch skill now supports per-task `--model` flag
- Init skill has working non-MCP bootstrap for fresh projects
- Remaining F-53 work: T-315–T-324 (phases 1.2–3.11), T-328, T-331
